{
  "name": "termech-document-registry",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "app:build": "node scripts/build-app.mjs",
    "prototype:build": "node scripts/build-standalone-prototype.mjs",
    "review:serve": "node scripts/serve-review.mjs",
    "dev": "npm run app:build && concurrently -k -n api,web -c yellow,cyan \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "build": "npm run prototype:build && npm run app:build && npm run build -w backend && npm run build -w frontend",
    "finn:validate": "node scripts/validate-finn-loop.mjs",
    "typecheck": "npm run typecheck -w backend && npm run typecheck -w frontend",
    "start": "npm run start -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "drizzle-orm": "^0.45.2"
  },
  "engines": {
    "node": ">=20"
  }
}
